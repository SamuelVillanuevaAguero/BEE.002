# 🐝 Bee - Monitor agent - Bee observa

Bee acts as an agent responsible for monitoring the execution status of bots under ROC’s supervision.
This project is a REST API developed using the FastAPI framework in Python.

# Getting Started
TODO: Guide users through getting your code up and running on their own system. In this section you can talk about:
1. run the next commands: `docker compose --profile dev up`
2. `docker-compose --profile dev up --build`
3. `alembic revision --autogenerate -m "initial"`

For new migrations run the next commands:
1. `alembic revision --autogenerate -m "description"`